# ⚛️ Atomic Chain: Evolution

**Atomic Chain** — это футуристическая вариация классической головоломки 2048, вдохновленная темой микромира и космической эволюции. Вместо обычных чисел игроку предстоит объединять фундаментальные частицы, превращая их в атомы, клетки и, в конечном итоге, в целые галактики.

---

## 🌌 Концепция игры
Игра разделена на **Stage (Этапы)**. Каждый этап — это новый виток эволюции. 
Вы начинаете с простейшего элемента текущего уровня (например, Кварка) и должны путем синтеза (объединения одинаковых элементов) достичь целевой частицы, чтобы перейти на следующий этап.

### Ключевые особенности:
* **Система прогресса**: Визуальная шкала (Progress Bar) показывает, насколько вы близки к завершению текущего этапа.
* **Динамическая сложность**: Настраиваемый конфиг позволяет менять количество шагов для победы.
* **Эффект супер-свечения**: Плитки высокого уровня начинают пульсировать неоновым светом, создавая атмосферу глубокого космоса.
* **Sidebar Эволюции**: Список всех доступных этапов с отображением прогресса и разблокированных элементов.

---

## 🛠 Технический стек
* **HTML5** (Семантическая разметка, контейнеры для сетки)
* **CSS3** (Flexbox/Grid, CSS Variables для тем, Keyframe анимации для пульсации)
* **Pure JavaScript** (ES6+, ООП подход, LocalStorage для сохранения прогресса)

---

## ⚙️ Настройка (GAME_CONFIG)
В файле `app.js` вынесен объект конфигурации для быстрого изменения баланса игры:

```javascript
const GAME_CONFIG = {
    evolutionStepsPerLevel: 3,     // Сколько объединений нужно до победы (например, +3 уровня от базового)
    spawnNewTileChance: 0.9,       // Шанс появления базовой плитки (90%)
    grid: {
        size: 4,                   // Размер поля (4x4)
        animationSpeed: 150        // Скорость движения плиток (мс)
    },
    glow: {
        threshold: 1,              // С какого уровня плитка начинает светиться
        maxOpacity: 0.8            // Максимальная яркость неона
    }
};
```

---


# 🧪 Цепочка эволюции (Elements)

Примеры элементов, через которые проходит игрок:

- **QUARK** (Кварк)  
- **PROTON** (Протон)  
- **ATOM** (Атом)  
- **CELL** (Клетка)  
- … и далее до **THE BIG BANG**

---

## 🚀 Как запустить

1. Склонируйте репозиторий.  
2. Откройте `index.html` в любом современном браузере.  
3. Используйте стрелки на клавиатуре или свайпы на телефоне для управления.

---

## 🏗 Разработка

Проект построен на классах.  
Основной класс **Game2048** управляет:

- Инициализацией сетки и обработкой ввода  
- Логикой перемещения и слияния плиток  
- Расчётом прогресса и сохранением состояния в `localStorage`  
- Обновлением UI (Progress Bar, Level Indicator, Sidebar)

